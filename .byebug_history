exit
Group.find(1).lists.find(15).items
n
Group.find(1).lists.find(15).itemsn
Group.find(1).lists.find(15).items
Group.find(1).lists.find(15)
Group.find(1).list.find(15)
Group.find(1)
@item
exit
@item
