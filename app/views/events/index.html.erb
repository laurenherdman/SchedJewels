<div class="simple_calendar">
	<%= month_calendar events: @events do |date, events| %>
  <%= date.strftime("%e") %>

  <% events.each do |event| %>
    <div>
      <%= event.name %>
    </div>
  <% end %>
<% end %>
</div>
